language: java

install: true
jdk: oraclejdk8

os: windows

notifications:
  email:
    - tom_gunners@hotmail.com
build:
    stage: build
    script:
      - mvn compile

test:
  stage: test
  script:
    - mvn clean test